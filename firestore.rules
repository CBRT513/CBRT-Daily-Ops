rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Emulator-only permissive rules; tighten before prod.
      allow read, write: if true;
    }
  }
}
